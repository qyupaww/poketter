name: poketter
description: "A Pokedex App build with Flutter."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Morpheme
  morpheme_base: ^2.1.0
  morpheme_cached_network_image: ^3.3.0
  morpheme_responsive: ^0.0.4
  morpheme_extension: ^1.1.1
  morpheme_http: ^6.1.0

  # Google Font
  google_fonts: ^8.0.1

  # Navigation
  go_router: ^17.1.0

  # Service Locator
  get_it: ^9.2.0

  # Storage
  flutter_secure_storage: ^10.0.0

  # Component
  skeletonizer: ^2.1.2
  flutter_svg: ^2.2.3
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  url_launcher: ^6.3.2

  # Utils
  intl: ^0.20.2
  async: ^2.13.0
  equatable: ^2.0.8
  collection: ^1.19.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  mocktail: ^1.0.4
  bloc_test: ^10.0.0
  fake_async: ^1.3.3

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/illustrations/




